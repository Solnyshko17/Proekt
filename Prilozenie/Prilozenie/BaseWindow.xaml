<Window x:Class="Prilozenie.BaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Prilozenie"
        mc:Ignorable="d"
        Title="BaseWindow" Height="800" Width="1300" 
        MinHeight="450" MinWidth="800" 
        MaxHeight="850" MaxWidth="1800">
    <Grid Background="#FF8EC3F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="btnEdit" Content="Выйти" Click="btnEdit_Click" HorizontalAlignment="Right" Margin="0 0 20 0"/>
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" Grid.RowSpan="2" Grid.Row="1"/>
    </Grid>
</Window>
